package net.geral.math;

public class GNMath {
	/*
	public static String toFracao(final double d) {
		final int v4 = (int)Math.round(d * 4.0); // round to .25 precision
		final int v1 = v4 / 4;
		final String s = (v1 == 0) ? "" : String.valueOf(v1);

		switch (v4 % 4) {
			case 0:
				return String.valueOf(v1); //if zero, return "0" (not "" in 's')
			case 1:
				return s + "¼";
			case 2:
				return s + "½";
			case 3:
				return s + "¾";
			default:
				return "[ERR" + (v4 % 4) + "]";
		}
	}
	*/
}
